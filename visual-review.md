# 视觉与可用性审查清单 · 汉教云程 / 心途领航官网

面向：前端主负责人
角色：视觉与可用性审查负责人（**只读源码，不改动网站源码**）
写入范围：本文件（`work/visual-review.md`）为本次唯一产出
上游依据：`work/content-plan.md`（内容分级 A/B/C/D 与标注组件）、`work/xintu.txt`、`work/ppt.txt`、`work/lead_prompt.txt`、`work/visual_prompt.txt`
审查时点：截至本文件写入，`work/site/` **尚未创建**，故本文先作为"落稿前设计约束"，交付后逐条转为验收结论

---

## 0. 三条不可协商的审查前提

在谈配色和动效之前，先定住三件事，否则后面所有视觉决策都会漂：

1. **视觉重量必须服从可信度分级。** 内容侧把数字分成 A（机构资料）/ B（行业公开数据，须署名）/ C（案例·参考·以实际岗位为准）/ D（禁止上屏）。视觉层面的规则是：
   > **数字的字号、颜色饱和度、发光效果，与其可信度等级成正比，不得倒挂。**
   A 类可以大、可以进首屏；C 类必须小而克制、必须与 `参考范围 · 以实际岗位为准` 同屏同视区；D 类不上屏。**任何 C 类数字出现在首屏 hero 即视为不通过。**
2. **"中国感"不等于"红+金"。** 本项目的受众是想转行、想出海就业的成年人，不是节庆消费者。红金高饱和会把他们带回"中介招生广告"的心理分类，直接削弱转化。中国感用**靛青底 / 纸张米白面 / 朱砂单点行动色 + 留白与网格秩序**来表达，详见 §3 与 §10。
3. **地图是数据可视化，不是装饰画。** 节点数、位置、状态必须来自真实口径（85 个国家和地区，不足者聚合为"其他地区"）。**装饰性假点、与数据无关的闪烁星点、无来源的弧线，一律不通过。**

---

## 1. 审查方法与执行顺序

| 阶段 | 动作 | 工具 | 通过判据 |
|---|---|---|---|
| S1 静态走查 | 三档视口截图（375 / 768 / 1280） | DevTools 设备模拟 | 无横向滚动、无文字溢出、无遮挡 |
| S2 对比度复核 | 取 token 实际渲染值测比 | axe DevTools / DevTools 取色 + 对比检测 | 正文 ≥ 4.5:1，大字 ≥ 3:1，图形/边框 ≥ 3:1 |
| S3 键盘走查 | 仅用 Tab/Shift+Tab/Enter/Esc/Space 走完全流程 | 手工 | 焦点不丢、不跳序、可见、Esc 可退、关闭后焦点归位 |
| S4 降级验证 | ① 开 `prefers-reduced-motion: reduce` ② 禁用 JS ③ 网络限速 Slow 3G | DevTools Rendering 面板 | ① 动效停止但状态仍可辨 ② 内容仍可读 ③ 首屏无长时间空白 |
| S5 放大验证 | 浏览器缩放 200% + 系统字号放大 | DevTools | 不重叠、不裁切、不出现横向滚动 |
| S6 合规视走 | 逐字核对 C 类数字的视觉标注是否与数字同视区 | 人工比对 `content-plan.md` 第 6 节 | 每处 C 类数字 100% 带标注 |

**打分建议**：每个领域 10 分制，任一"红线项"（§12 中标 🔴）不通过，则该领域判定为不通过，不论总分。

---

## 2. 配色审查清单

### 2.1 检查项

- [ ] 全站色相数 ≤ 4（靛青族 / 米白族 / 朱砂 / 青瓷），无第五种装饰色
- [ ] 朱砂橙**只用于行动与强调**（主 CTA、当前步骤、焦点环、C 类数字标注），不做大面积背景、不做标题渐变
- [ ] 深色区与米白区交替出现，形成"暗—亮—暗"节奏；不允许整页同底色
- [ ] 深色底上不使用纯黑 `#000` 与纯白 `#fff`（对比过冲，长时间阅读刺眼）
- [ ] 米白面使用纸张质感米白（`#FBF8F1` 系），不用纯白卡片浮在灰底上
- [ ] 分隔线使用 hairline（透明度描边），不用 1px 实心灰线
- [ ] 状态色（成功/警示/信息）与品牌色分离，不借用朱砂表达"错误"
- [ ] 圆角不统一：卡片 / 面板 / 标签 / 地图容器使用不同的半径档位（见 §6 半径矩阵）

### 2.2 红线

- 🔴 **红金组合**：正红（`#C8102E` 一类）大面积 + 金色渐变标题 / 金色描边按钮 / 光晕，视为不通过
- 🔴 **金色渐变文字**（`background-clip: text` 配金渐变）——本项目的可信度语境下必须移除
- 🔴 **低对比灰字**：浅灰 `#999` 放在米白上（约 2.8:1）——正文与小字均不通过
- 🔴 **用颜色单独表达状态**（如仅用红点表示"即将截止"）——不通过，需叠加文字或形状

### 2.3 推荐色板（已按使用场景分工，落地值见 §11）

| 角色 | token | 值 | 用法 | 对比度参考 |
|---|---|---|---|---|
| 深墨蓝底 | `--ink-900` | `#0B1A2B` | 首屏、地图区、页脚 | — |
| 深墨蓝层次 | `--ink-800` / `--ink-700` | `#102538` / `#17324A` | 面板、卡片（深色区） | — |
| 浅墨蓝文字 | `--ink-500` | `#33556F` | 米白面上的次要文字 | 与 paper-050 ≈ 7.2:1 |
| 纸张米白 | `--paper-050` | `#FBF8F1` | 内容区主面 | — |
| 米白分层 | `--paper-100` / `--paper-200` | `#F4EFE4` / `#E8E0D0` | 交替行、分区底 | — |
| 朱砂橙（行动） | `--cinnabar-500` | `#C8451C` | 主按钮底、强调 | 与 paper-050 ≈ 4.6:1 ✅ |
| 朱砂橙（深底上的文字/图形） | `--cinnabar-400` | `#E2603A` | 深底上的强调文字、焦点环 | 与 ink-900 ≈ 5.0:1 ✅ |
| 青瓷绿（数据/可信） | `--celadon-500` | `#2E7D6F` | 步骤完成态、数据标签 | 与 paper-050 ≈ 4.6:1 ✅ |
| 深底主文字 | `--text-on-dark` | `#F5F1E8` | 深色区正文 | 与 ink-900 ≈ 15:1 ✅ |
| 深底次要文字 | `--text-on-dark-muted` | `#A9BACB` | 深色区说明文字 | 与 ink-900 ≈ 8.8:1 ✅ |
| 浅底主文字 | `--text-on-light` | `#16202B` | 内容区正文 | 与 paper-050 ≈ 15.5:1 ✅ |
| 浅底次要文字 | `--text-on-light-muted` | `#4E5D6B` | 内容区说明文字 | 与 paper-050 ≈ 6.4:1 ✅ |

> 上表比值为按 sRGB 相对亮度估算，**S2 阶段需用工具以实际渲染色复核**（含透明度叠加后的实际值）。

### 2.4 三级标注组件的视觉规范（承接 `content-plan.md` §0）

这三类标注是本次项目的合规生命线，**不能做成 10px 的浅灰小字**：

| 组件 | 文案 | 视觉规则 |
|---|---|---|
| `机构资料` 角标 | `机构资料 · 截至 2026-09` | 8–12px 圆角方标，青瓷绿描边 + 同色文字，字号 ≥ 12px，不可用透明度过低 |
| `行业参考` 角标 | `行业参考 · 来源：___` | **来源为空则整个数字不上屏**；青瓷绿底浅填充 |
| `参考范围` 标注 | `参考范围 · 以实际岗位为准` | 朱砂描边、字号 ≥ 12px、与 C 类数字**同容器、同视区、不折叠隐藏**；禁止只放页脚 |
| 考试口径标签 | `ICA 考试资料口径` | 朱砂点状描边 + `ⓘ` 图形；**必须同时有可见文字**，不能只做 hover tooltip |

实现要求：三类标注必须是**真实 DOM 文本**（可被屏幕阅读器读到），不得用 `::before` 背景图或 `title` 属性承载关键信息。

---

## 3. 字体审查清单

### 3.1 检查项

- [ ] 标题使用**中文衬线/serif 气质**字体，正文使用**清晰无衬线**；同一层级的字体家族在全站唯一
- [ ] 衬线只用于标题与少量引文，**不用于正文与小字**（CJK 衬线小字号在低分屏糊）
- [ ] 正文中文行高 ≥ 1.7（CJK 需要比拉丁文更大行距），标题行高 1.2–1.35
- [ ] 每行中文 28–38 字（`max-width: 34em` 量级），不允许整行拉满容器
- [ ] 字重使用 ≤ 4 档（400 / 500 / 600 / 700），不出现 `font-weight: 900` 的"宣传体"
- [ ] 标题**不使用负字距**（CJK 负字距会挤成一团）；标题字距 0，仅在标签/全大写拉丁处用 `0.08em`–`0.16em`
- [ ] 数字与拉丁字母使用与正文协调的字族，或启用 `font-variant-numeric: tabular-nums`（数据表、步骤号必须对齐）
- [ ] 字号不随视口无限缩放：使用 `clamp()` 并设上下限
- [ ] 正文字号在移动端 ≥ 16px，标签类小字 ≥ 12px（绝对下限，不可再小）
- [ ] 字体栈**不依赖远程字体文件**（与 `lead_prompt.txt` 的"不依赖远程图片/避免安装"一致），使用系统字体栈并提供 serif/sans 双回退

### 3.2 红线

- 🔴 正文使用花体/手写体/艺术字（"毛笔字标题"是本项目的高频模板化风险）
- 🔴 `font-size` 小于 12px 的合规标注（把 `以实际岗位为准` 缩到 10px 灰字，等于隐去）——**视为变相规避标注，不通过**
- 🔴 全站标题使用渐变 + 描边 + 阴影三件套叠加

### 3.3 字体栈建议

```css
/* 标题：中文衬线优先，回退到系统 serif */
--font-serif: "Songti SC", "SimSun", "Source Han Serif SC", "Noto Serif SC", Georgia, "Times New Roman", serif;
/* 正文：系统中性无衬线，保证 Windows/Mac/移动端一致 */
--font-sans: system-ui, -apple-system, "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans SC", sans-serif;
/* 数字/数据展示：等宽数字，避免跳动 */
--font-num: "SF Mono", "Cascadia Mono", Consolas, "Roboto Mono", ui-monospace, monospace;
```

> 说明：`Songti SC` / `SimSun` 是中文系统自带衬线，无需下载字体文件即可获得"有气质的标题"；Windows 上 `SimSun` 渲染偏细，建议标题只到 600 字重并配合 0.01em 字距微调。若后续允许内嵌字体，再升级为思源宋体子集（`woff2` + `font-display: swap`），但**不得阻塞首屏**。

---

## 4. 布局节奏审查清单

### 4.1 节奏原则：宽—窄—宽，暗—亮—暗，密—疏—密

不允许"每节都是全宽 + 居中 + 三等分卡片"。全站必须形成可感知的节奏差：

| 段落 | 底色 | 版心 | 密度 | 意图 |
|---|---|---|---|---|
| 首屏 | 深墨蓝 + 星图/地图动势 | 宽（近满屏） | 疏（大留白 + 一句话） | 建立定位与动势 |
| 证据带 | 深墨蓝延续 | 宽 | 密（4 个 A 类数字横排） | 快速建立可信 |
| 模块一 全球岗位地图 | **深色 canvas** | 近满屏，地图可溢出容器 | 密（节点可点） | 探索感 |
| 模块二 取证通关路径 | **米白纸面** | 中（`--container-narrow`） | 中（轨道 + 展开面板） | 秩序感、可读 |
| 模块三 就业陪跑 | 米白 → 深色过渡 | 中宽 | 中（时间线纵向推进） | 过程感 |
| 小测验 | 米白 + 抬升面板 | 窄 | 密 | 交互焦点 |
| FAQ | 米白 | 窄 | 疏 | 消解疑虑 |
| 页脚 CTA | 深墨蓝 | 宽 | 疏 | 收束 |

### 4.2 检查项

- [ ] 相邻两个 section 的**底色或版心宽度至少有一项不同**；不允许连续 3 节同底色同版心
- [ ] 垂直节奏使用统一刻度（4px 基数）：`--space-*`，节间用 `--section-y` 且允许在移动端收缩
- [ ] 容器有明确上限：`--container: 1200px` / `--container-narrow: 760px`，正文另有 `--measure: 34em` 约束
- [ ] 首屏之后紧接**数字证据带**，且这 4 个数字必须是 A 类（2021 / 10000+ / 300+ / 85），每个带 `机构资料` 角标
- [ ] 模块间有**过渡带**（细线 + 时间轴刻度 / 地图经纬线延伸），而不是硬切
- [ ] 网格对齐：桌面 12 列，平板 8 列，移动 4 列；模块内元素左右边缘对齐到同一列线
- [ ] 卡片只用于**平行且等权**的信息；否则改用主卡 + 附属行、时间线、轨道等非对称结构
- [ ] 全站不出现"三张同尺寸圆角卡 + 三个线性图标 + 三个加粗短句"的固定组合 ≥ 2 次

### 4.3 半径与描边矩阵（反"一律 16px 圆角"）

| 元素 | token | 值 | 理由 |
|---|---|---|---|
| 地图容器 / 全宽视觉 | `--radius-canvas` | `0` | 图面应边界清晰，不被圆角削弱 |
| 内容面板 / 结果面板 | `--radius-panel` | `20px` | 主面板最大的圆角，体现层级 |
| 普通卡片 | `--radius-card` | `12px` | 次级 |
| 内嵌小卡 / 展开项 | `--radius-inset` | `8px` | 更内层更小 |
| 标注角标 / 标签 | `--radius-tag` | `6px` | 方标，与卡片区分 |
| 按钮 | `--radius-btn` | `10px` | 不用胶囊，避免"营销页"观感 |
| 头像 / 圆形指示 | `--radius-full` | `999px` | 仅用于真的圆形元素 |

**规则：内层圆角 ≤ 外层圆角**，禁止内外层同值（同值会产生"糊在一起"的模板感）。

---

## 5. 首屏构图审查清单

### 5.1 构图公式（桌面）

```
[ 顶部锚点导航 · 高 ≤ 64px · 半透明深底 + hairline 下边 ]
────────────────────────────────────────────────────
左 55%                          右 45%
H1（衬线，2 行内）              地图 / 星图动势
副标题（1–2 行，说明性质）        · 85 国节点（真实数据）
主 CTA + 次 CTA                  · 极细经纬线 / 航线弧
信任行：4 个 A 类数字 + 角标      · 节点可聚焦、可点击
────────────────────────────────────────────────────
[ 滚动提示 / 模块一预告 ]
```

**移动端（375px）**：H1 → 副标题 → 主 CTA →（次 CTA）→ 信任数字横滑或 2×2 网格 → 地图下沉到模块一。**首屏不塞地图**，避免小屏点不准。

### 5.2 检查项

- [ ] 首屏**一句话说清三件事**：教什么（国际中文教师）、给谁（转行/副业/出海）、帮到哪一步（认证培训 → 取证 → 岗位对接）
- [ ] H1 ≤ 14 个汉字为佳，2 行内收；不出现"重新定义你的人生""财富与视野双重突破"类 PPT 原始话术（属 D 类）
- [ ] 主 CTA 唯一且文案为动词短语（`查看全球岗位地图`），次 CTA 是探询式（`我的条件能对接到哪类岗位？`）
- [ ] 首屏数字**只允许 A 类**且全部带 `机构资料` 角标；**C 类薪资数字禁止出现在首屏**
- [ ] 动势来自**结构**（地图节点、航线、经纬网格），不来自**无意义漂浮粒子**
- [ ] 首屏内容不依赖 JS 渲染：禁 JS 时 H1/副标题/CTA/数字仍完整可见（**禁止用 `opacity:0` + JS 入场作为可见前提**）
- [ ] 顶部导航锚点 ≥ 4 项，当前区块高亮；滚动后导航收缩但**不遮挡首屏 CTA**
- [ ] 无倒计时、无"仅剩 X 名额"、无滚动后突然弹出的浮层（暗黑模式，直接不通过）
- [ ] 375×667 下首屏**无横向滚动**，H1 不出现单字成行
- [ ] 深色首屏与下方米白内容区的接缝处理明确（斜切 / 渐变 / 细线三选一，不允许生硬直角切）

### 5.3 红线

- 🔴 首屏大号金色/红色"年薪 40–60 万""时薪 550 元"作为视觉主体
- 🔴 首屏出现未授权第三方平台 Logo 墙（Lingoace / iTalki 等）——`content-plan.md` §2 已明确"未获授权前一律不出现品牌标识"
- 🔴 首屏自动轮播（无暂停控件、间隔 < 5s）

---

## 6. 三模块互动审查清单

### 6.1 模块一 · 全球岗位地图

| 检查项 | 通过标准 |
|---|---|
| 数据真实 | 节点对应 85 个国家和地区口径；不足部分聚合成"其他地区"，**禁止装饰性假点** |
| 可点击 | 节点为真实可聚焦元素（`<button>` 或 SVG 内可聚焦元素 + `tabindex="0"`），`Enter`/`Space` 均可触发 |
| 面板内容 | 固定三栏：岗位类型 / 工作方式（全职·兼职·远程）/ 参考收入范围 |
| 合规标注 | 面板内 `参考范围 · 以实际岗位为准` **常驻显示、不可折叠、不可只在 tooltip** |
| 键盘可达 | Tab 顺序与视觉顺序一致；`Esc` 关闭面板并把焦点还给触发的节点 |
| 等价路径 | 提供"列表视图"开关（地图不可作为唯一获取路径），SR 用户可读的 `<ul>` 结构 |
| 移动端 | 地图在 ≤ 768px 退化为**按地区分组的可展开列表**，不要求精细点按，不依赖 hover |
| 点击反馈 | 节点有选中态（描边 + 标签），且选中态不只用颜色表达 |
| 性能 | 节点动画只用 `transform`/`opacity`；节点数多时避免逐节点 DOM 动画 |

**反模式**：3D 旋转地球（性能差 + 诱发眩晕 + 点不准）；无数据支撑的随机闪烁星点；节点点击后弹出**仅含薪资**的面板（把工具做成薪资诱饵）。

### 6.2 模块二 · 取证通关路径

| 检查项 | 通过标准 |
|---|---|
| 结构表达 | 用**有序步骤**（`<ol>`）表达 `报名咨询 → 笔试准备 → 笔试合格 → 面试/微格训练 → 面试 → 取证 → 岗位对接` |
| 禁止通过率暗示 | **不得**使用"你的通过进度 80%"类环形进度条；步骤序号用 `03 / 07` 表达位置 |
| 口径标签 | 笔试、面试两个节点常驻 `ICA 考试资料口径` 标签，且有可见文字说明（非 hover-only） |
| 时效提示 | 所有分值/时长/合格线（100 分、60 分、约 15 分钟）**与"报名前以报考机构官网最新公告为准"同屏出现**，不允许只放页脚 |
| 证书分栏 | 汉办 / IPA / ICA **三种体系分栏说明**，禁止合并为"官方证书"一个词 |
| 展开交互 | 每步可展开：考点范围、训练方式、时长参考、规则提示；展开状态 `aria-expanded` 同步 |
| 键盘 | 步骤条可 Tab 遍历，方向键可选（推荐），`Home`/`End` 支持（可选） |
| 移动端 | 横向轨道在移动端转为纵向时间线，避免横向滚动依赖 |
| 完成态 | `celadon-500` 表示已完成步骤，且**同时**有文字/图标变化 |

**反模式**：把培训课程包装成"官方授权培训"（无授权文件时不允许）；进度条暗示通过率；"通关率直线飙升"上屏（D 类，禁止）。

### 6.3 模块三 · 就业陪跑与真实案例

| 检查项 | 通过标准 |
|---|---|
| 时间线结构 | 四段式（简历精修 → 试讲特训 → 模拟面试 → 谈薪技巧），用 `<ol>` 语义化，进度以纵向推进线表达 |
| 动效目的 | 推进线随滚动/展开推进，表达"流程前进"，**不做无意义循环动画** |
| 承诺的视觉克制 | 零抽成 / 直签 / 薪资直达个人账户：使用**条款式排版**（编号 + 短句 + 条件说明），不用金色徽章、不用大号担保字体 |
| 条款可验证 | 每项承诺旁可跳转/展开"协议要点"，且注明"具体签约主体以岗位方为准" |
| 小测验 | 3–5 题，单选为主；结果面板给三选一建议（国内全职 / 线上兼职 / 海外岗位） |
| 结果免责 | 结果面板**必须**带"建议仅供参考，实际以岗位要求与个人条件为准"，字号 ≥ 14px |
| 无暗黑模式 | 不强制留手机号邮箱可看结果；不伪造"还有 X 人正在测" |
| 案例区 | 无授权素材时渲染**空态占位**（"案例整理中"），**禁止假头像、假姓名首字母、假公司 Logo** |
| 结果可访问 | 结果面板 `role="status"` / `aria-live="polite"`，键盘可达，`Esc` 关闭并归还焦点 |
| 移动端 | 时间线在移动端为单列纵向；触控目标 ≥ 44×44px |

**反模式**：用假数据凑满案例区（`xintu.txt` §4.3/4.4 原文为图片/空白位，**无任何可引用个案**，这是本次项目的硬约束）；把个案渲染成大号"学员月入 X 万"卡片。

---

## 7. 移动端审查清单

| # | 检查项 | 通过标准 |
|---|---|---|
| 1 | 断点 | 至少覆盖 360 / 480 / 768 / 1024 / 1280，且 768 与 1024 之间无破版 |
| 2 | 横向滚动 | `body` 不出现横向滚动；长英文/数字用 `overflow-wrap: anywhere` 兜底 |
| 3 | 触控目标 | 所有可点元素 ≥ 44×44px（建议 48），含 CTA、节点、折叠头、FAQ 行 |
| 4 | 视口设置 | **禁止** `user-scalable=no` / `maximum-scale=1`（会阻断缩放，属无障碍失败） |
| 5 | 字号 | 正文 ≥ 16px；`-webkit-text-size-adjust: 100%` |
| 6 | 首屏 | 375×667 内可见 H1 + 副标题 + 主 CTA；地图下沉而非压缩 |
| 7 | 导航 | 移动端导航高 ≤ 56px，滚动收缩；锚点跳转需 `scroll-margin-top` 避免被导航盖住 |
| 8 | 吸底 CTA | 若使用，必须 `padding-bottom: env(safe-area-inset-bottom)`，且不遮挡 FAQ/页脚内容 |
| 9 | 交互模型 | 全站**不依赖 hover**；所有 hover 信息在 tap 或常驻可见 |
| 10 | 性能 | 首屏无远程图片/字体阻塞；SVG 自绘且体积可控；动效不影响滚动流畅度 |
| 11 | 手势 | 不劫持系统手势（左右滑返回）；横向滚动区域（若有）需有视觉提示 |
| 12 | 展开态 | 折叠/展开在移动端保持滚动位置稳定，不出现"展开后跳回顶部" |

---

## 8. 无障碍审查清单（WCAG 2.2 AA 对齐）

**感知**
- [ ] 文本对比度 ≥ 4.5:1；大字（≥24px 或 ≥18.66px 粗体）≥ 3:1
- [ ] 非文本对比度（图标、边框、地图节点、进度线）≥ 3:1
- [ ] 颜色不是唯一的信息载体（步骤状态、节点状态、必填标识均需文字/形状辅助）
- [ ] SVG 装饰元素 `aria-hidden="true"`；语义图形加 `role="img"` + `<title>`
- [ ] 200% 缩放与 400% 缩放（320px 宽度）下内容不丢失、不需双向滚动

**可操作**
- [ ] 全部功能键盘可达，Tab 顺序 = 视觉顺序
- [ ] 焦点可见：**双层焦点环**（描边 + 光晕），在深底与米白底上均可辨
- [ ] 无键盘陷阱；面板/弹层关闭后焦点返回触发元素
- [ ] 跳转链接（Skip to content）作为首个可聚焦元素
- [ ] 目标尺寸 ≥ 24×24px（WCAG 2.2 新增），本项目建议直接按 44×44 执行

**可理解**
- [ ] `<html lang="zh-CN">`；中英混排标注 `lang="en"`（如平台名）
- [ ] 一页一个 `<h1>`，标题层级不跳级
- [ ] 每节用 `<section aria-labelledby="...">` 关联标题
- [ ] 折叠项使用 `<details>/<summary>` 或 `aria-expanded` + `aria-controls`
- [ ] 咨询面板每个输入有 `<label>` 关联；错误提示用文字描述，不只变红
- [ ] 链接文案有语义（避免"点击这里"）

**健壮**
- [ ] toast / 结果面板 / 表单反馈：`role="status"` 或 `aria-live="polite"`（不用 `assertive` 打断）
- [ ] 合规标注（机构资料 / 参考范围 / 考试口径）是真实 DOM 文本，SR 可读
- [ ] 表单控件有 `autocomplete` 与合适的 `inputmode`
- [ ] 无 JS 时核心内容（三大模块的静态说明、合规标注）仍可读

**红线**
- 🔴 焦点样式被 `outline: none` 移除且无替代
- 🔴 地图节点只能鼠标点击（键盘用户无法获取任何岗位信息）
- 🔴 `参考范围 · 以实际岗位为准` 只在视觉上存在、SR 读不到

---

## 9. 动效审查清单

### 9.1 原则

**每个动效必须能回答"它表达了什么状态变化"。** 回答不了就删掉。

| 类别 | 允许 | 禁止 |
|---|---|---|
| 状态反馈（点击、展开、选中） | 140–240ms，`ease-out`，只动 `transform`/`opacity` | 长回弹、多属性同时动 |
| 入场（滚动进入视口） | ≤ 420ms，`translateY(12–20px)` + 淡入，**只播一次** | 逐元素长串延迟（每元素 > 120ms 延迟）、无限循环 |
| 地图节点 | 选中态缩放/描边过渡；极克制的呼吸（仅限 1–2 个"重点地区"） | 全节点随机闪烁、大面积粒子漂浮 |
| 滚动进度 | `transform: scaleX()` 驱动，rAF 节流或 scroll-driven | 用 `width`/`height` 触发布局抖动 |
| 过渡带 | 细线延伸、经纬网格淡入 | 视差滚动（parallax）、大幅位移 |
| 加载态 | 骨架/SVG 线稿动画 | 无限 spinner 伴随空屏 |

### 9.2 检查项

- [ ] 时长集中在 140–420ms；微反馈 ≤ 160ms；入场 ≤ 420ms
- [ ] 缓动统一使用 `--ease-out` / `--ease-in-out`，不混入 spring 过度回弹
- [ ] 只动 `transform` 与 `opacity`；不出现动画属性含 `top/left/width/height/margin`
- [ ] `will-change` 谨慎使用（仅动画期间），不常驻
- [ ] `prefers-reduced-motion: reduce` 下：**关闭**漂浮/视差/自动轮播/连续循环动画；**保留** ≤ 150ms 的透明度过渡与所有不依赖动画的状态变化
- [ ] 内容可见性**不依赖动画完成**：JS 失败/慢网时 H1、CTA、数字、合规标注必须可见
- [ ] 入场动画使用 `IntersectionObserver`，触发后 `unobserve`，不重复播放
- [ ] 无自动播放轮播；如保留，提供暂停且间隔 ≥ 5s
- [ ] toast 自动消失时间 ≥ 5s，或有手动关闭，且不遮挡关键内容
- [ ] 动效不引起 CLS：预留尺寸，避免入场位移推动布局

### 9.3 红线

- 🔴 内容默认 `opacity: 0`，靠 JS 才显示（无 JS 即白屏）
- 🔴 无视 `prefers-reduced-motion`
- 🔴 大面积持续视差 / 3D 旋转地球（性能 + 前庭不适）
- 🔴 无限循环的"呼吸光晕"覆盖在文字上影响阅读

---

## 10. 模板化风险与替代方案（本次重点）

### 风险一：红金宣传页

**症状识别**
- 大面积正红底 / 红金撞色，金色渐变标题（`background-clip: text`）
- 无处不在地贴祥云、灯笼、毛笔、印章、龙纹
- 光效、发光、金属描边按钮
- 大字号"财富""高薪""飞跃"话术与红金底色绑定

**为什么必须改**
受众是**正在考虑转行的成年人**。红金高饱和页面的心理归类是"节庆促销 / 招生中介广告"，与"职业发展与国际就业"的定位直接冲突；同时高饱和红会与"中介抽成"的负面联想绑定——而这恰恰是本项目要强调"零抽成"反驳的点。**红金底 + 零抽成承诺是自相矛盾的视觉组合。**

**替代方案**
1. **底色换成靛青/深墨蓝**（`--ink-900`），让页面在同类竞品中第一眼就区分为"专业服务"而非"促销"
2. **内容面用纸张米白**（`--paper-050`），形成"深色导航/首屏 + 米白阅读面"的节奏
3. **朱砂橙只做单点行动色**（主 CTA、焦点环、当前步骤、C 类标注），全站朱砂覆盖面积控制在 **5% 以内**
4. **中国感改由结构与细节表达**：留白比例、网格秩序、竖排小标题、印章式方角标签（`--radius-tag: 6px`）、hairline 描边
5. **金色最多保留一处**，用作极细装饰线（`1px`，透明度 ≤ 0.35），不做渐变、不做描边、不做文字

### 风险二：重复卡片（卡片疲劳）

**症状识别**
- 每个 section 都是"3–4 个同尺寸、同圆角、同阴影、同图标位"的卡片网格
- 全站圆角只有一个值（例如处处 `16px`），所有卡片阴影参数相同
- 卡片内部结构高度一致：图标 + 加粗短句 + 两行说明
- 用户滚到第三屏后无法分辨自己身在第几节

**为什么必须改**
三模块的信息结构本质**不同**：模块一是**空间/探索型**（地图）、模块二是**顺序/流程型**（步骤）、模块三是**时间/推进型**（时间线）。用同一种卡片容器装三种结构，等于抹掉了信息本身的形状，用户只能靠读文字分辨差异，浏览效率与记忆点双降。

**替代方案**
1. **按信息形状选容器**：
   - 模块一 → **全宽深色 canvas + 可点节点 + 侧栏面板**（探索型）
   - 模块二 → **横向轨道 / 纵向时间线 + 可展开步骤**（顺序型）
   - 模块三 → **纵向推进时间线 + 单据式条目**（推进型）
2. **卡片只用于"平行且等权"的信息**（例如四个陪跑辅导若并列展示），且同一视图内卡片组不超过 1 组
3. **引入非对称布局**：主卡 + 附属行、左图右文、大数字 + 小说明的组合
4. **半径差异化矩阵**（见 §4.3）：内层圆角小于外层，且容器类型不同则半径不同
5. **阴影差异化**：深色区改用 hairline 描边 + 轻微内发光；米白区才用低强度投影（`0 1px 2px rgba(11,26,43,.06), 0 8px 24px rgba(11,26,43,.05)`）
6. **底色交替**：连续两节不得同底色（暗—亮—暗）

### 风险三：夸张薪资承诺

**症状识别**
- 首屏 hero 位置放"年薪 40–60 万""时薪 550 元""薪资飞跃式增长"
- 薪资数字使用超大字号 + 高饱和色 + 发光效果
- 把某一地区的时薪表述为"平均收入"或隐含"你也能拿到"
- 把 `xintu.txt` §4.2 的宣传话术直接上屏（"财富与视野的双重突破""价值将被世界重新定义"）
- 个案被渲染成大号"学员月入 X 万"卡片

**为什么必须改**
1. **合规**：`content-plan.md` 已将该类内容定为 **C 类（参考范围）** 与 **D 类（禁止上屏）**；宣传薪资写成普遍保证属明确红线
2. **可信**：夸张数字会触发成年受众的"招聘套路"警觉，**反而降低转化**
3. **信息设计**：薪资是"探索后的答案"，不是"入口的诱饵"。把它放在入口，用户读完就走，不看服务流程

**替代方案**

| 位置 | 现在（风险） | 改为 |
|---|---|---|
| 首屏 hero | 年薪 40–60 万大字 | 改用 **A 类机构数据**：2021 成立 / 累计培养 10000+ / 300+ 合作院校机构 / 覆盖 85 个国家地区，每项带 `机构资料` 角标 |
| 首屏 CTA | "看你能赚多少" | `查看全球岗位地图` + `我的条件能对接到哪类岗位？` |
| 薪资出现位置 | 首屏 + 多个 section 反复出现 | **只在模块一地图节点面板内出现一次**，随地区切换而变 |
| 薪资视觉权重 | 大号 + 朱砂 + 发光 | 字号 ≤ 该面板 H3 级别；色用 `--text-on-dark`（不用朱砂）；无发光、无渐变 |
| 薪资文案 | "美国时薪 350–550 元" | `参考范围 · 以实际岗位为准` 常驻同视区 + `来源：机构资料整理，非平台均薪` |
| 行业宏观数字 | 190 国 / 2 亿 / 3 千万 挂在"我们培养"语境 | 若保留，退到次级段落，**必须署名来源**；无来源则整套删除 |
| 个案 | 假头像 + 月入 X 万 | 无授权素材 → 空态"案例整理中"；有授权 → 用小号卡片 + 授权说明 + 参考标注 |
| D 类话术 | 直接抄 PPT | `通关率直线飙升` `薪资飞跃式增长` `价值将被世界重新定义` **不上屏**；改为可验证的服务动作（提供 N 次模拟、N 次批改） |

**一句话判据**：*如果把这个数字上的"参考范围 · 以实际岗位为准"删掉，页面会变得更有吸引力——那它就放错了位置或放大了权重。*

---

## 11. 可直接交给前端的 CSS Token

> 放在全局样式最上方，所有组件只引用 token，不写裸值。注释保留分级说明，避免后续被误用。

```css
:root {
  /* ============ 1. 色板 ============ */
  /* 深墨蓝：首屏 / 地图 / 页脚 */
  --ink-900: #0B1A2B;
  --ink-800: #102538;
  --ink-700: #17324A;
  --ink-600: #22405C;
  --ink-500: #33556F;

  /* 纸张米白：内容面 */
  --paper-050: #FBF8F1;
  --paper-100: #F4EFE4;
  --paper-200: #E8E0D0;
  --paper-300: #D8CDB8;

  /* 行动 / 强调（全站覆盖面积 ≤ 5%） */
  --cinnabar-600: #A83714;  /* hover / active */
  --cinnabar-500: #C8451C;  /* 浅底上的按钮底、强调文字 */
  --cinnabar-400: #E2603A;  /* 深底上的文字 / 图形 / 焦点环 */
  --cinnabar-100: #F6DDD3;  /* 浅底标注填充 */

  /* 数据 / 可信（标注、完成态） */
  --celadon-600: #236257;
  --celadon-500: #2E7D6F;
  --celadon-100: #D8E8E3;

  /* 文字 */
  --text-on-dark: #F5F1E8;
  --text-on-dark-muted: #A9BACB;
  --text-on-light: #16202B;
  --text-on-light-muted: #4E5D6B;

  /* 语义映射 */
  --bg-dark: var(--ink-900);
  --bg-light: var(--paper-050);
  --surface-dark: var(--ink-800);
  --surface-light: #FFFFFF;
  --action: var(--cinnabar-500);
  --action-hover: var(--cinnabar-600);
  --action-on-dark: var(--cinnabar-400);
  --data-accent: var(--celadon-500);

  /* 描边（hairline，替代实线分割） */
  --hairline-dark: rgba(255, 255, 255, 0.10);
  --hairline-dark-strong: rgba(255, 255, 255, 0.18);
  --hairline-light: rgba(11, 26, 43, 0.12);
  --hairline-cinnabar: rgba(200, 69, 28, 0.45);
  --hairline-celadon: rgba(46, 125, 111, 0.45);

  /* 阴影（深色区用描边，浅色区才用投影） */
  --shadow-card: 0 1px 2px rgba(11, 26, 43, 0.06), 0 8px 24px rgba(11, 26, 43, 0.05);
  --shadow-panel: 0 2px 4px rgba(11, 26, 43, 0.08), 0 24px 48px rgba(11, 26, 43, 0.12);
  --glow-selected: 0 0 0 1px var(--cinnabar-400), 0 0 16px rgba(226, 96, 58, 0.28);

  /* 焦点（双层环，深底浅底均可见） */
  --focus-color: #E2603A;
  --focus-ring-width: 3px;
  --focus-ring-offset: 2px;
  --focus-halo: rgba(226, 96, 58, 0.35);

  /* ============ 2. 字体 ============ */
  --font-serif: "Songti SC", "SimSun", "Source Han Serif SC", "Noto Serif SC", Georgia, "Times New Roman", serif;
  --font-sans: system-ui, -apple-system, "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", "Noto Sans SC", sans-serif;
  --font-num: "SF Mono", "Cascadia Mono", Consolas, "Roboto Mono", ui-monospace, monospace;

  --fs-display: clamp(2.25rem, 5.6vw, 4rem);     /* H1，首屏 */
  --fs-h2: clamp(1.625rem, 3.2vw, 2.5rem);      /* 模块标题 */
  --fs-h3: clamp(1.125rem, 1.6vw, 1.5rem);      /* 卡片/面板标题 */
  --fs-body-lg: clamp(1.0625rem, 1.15vw, 1.1875rem); /* 首屏副标题、导语 */
  --fs-body: 1rem;                              /* 正文（移动端不得小于此值） */
  --fs-small: 0.875rem;                         /* 辅助说明 */
  --fs-label: 0.8125rem;                        /* 标注角标（绝对下限 12px） */

  --fw-regular: 400;
  --fw-medium: 500;
  --fw-semibold: 600;
  --fw-bold: 700;

  --lh-display: 1.22;
  --lh-heading: 1.32;
  --lh-body: 1.75;      /* CJK 正文行高下限 */
  --lh-tight: 1.5;

  --ls-heading: 0.01em; /* CJK 标题微松，禁用负字距 */
  --ls-body: 0.01em;
  --ls-label: 0.08em;   /* 仅用于短标签/全大写拉丁 */

  /* ============ 3. 尺度与网格 ============ */
  --space-1: 0.25rem;   /* 4 */
  --space-2: 0.5rem;    /* 8 */
  --space-3: 0.75rem;   /* 12 */
  --space-4: 1rem;      /* 16 */
  --space-5: 1.5rem;    /* 24 */
  --space-6: 2rem;      /* 32 */
  --space-7: 3rem;      /* 48 */
  --space-8: 4rem;      /* 64 */
  --space-9: 6rem;      /* 96 */
  --space-10: 8rem;     /* 128 */

  --container: 1200px;
  --container-narrow: 760px;
  --measure: 34em;                              /* 正文行宽（28–38 汉字） */
  --gutter: clamp(1rem, 4vw, 2.5rem);
  --section-y: clamp(3.5rem, 8vh, 7.5rem);      /* 纵向节奏 */
  --section-y-compact: clamp(2.5rem, 5vh, 4rem);/* 密节奏段落 */

  --nav-h: 64px;
  --nav-h-mobile: 56px;
  --tap-min: 44px;                              /* 触控目标下限 */

  /* ============ 4. 圆角（差异化，禁止一律同值） ============ */
  --radius-canvas: 0px;
  --radius-panel: 20px;
  --radius-card: 12px;
  --radius-inset: 8px;
  --radius-tag: 6px;
  --radius-btn: 10px;
  --radius-full: 999px;

  /* ============ 5. 动效 ============ */
  --dur-instant: 120ms;   /* 微反馈 */
  --dur-fast: 160ms;
  --dur-base: 240ms;      /* 状态切换 */
  --dur-slow: 420ms;      /* 入场（上限） */
  --ease-out: cubic-bezier(0.22, 0.61, 0.36, 1);
  --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
  --delay-step: 60ms;     /* 列表错峰上限，逐项不超 60ms */

  /* ============ 6. 层级 ============ */
  --z-base: 0;
  --z-sticky: 100;
  --z-panel: 200;
  --z-nav: 300;
  --z-toast: 500;
  --z-modal: 600;
}

/* 双层焦点环：深色/浅色背景均可辨，禁止 outline:none 无替代 */
:where(a, button, [tabindex], summary, input, select, textarea, [role="button"]):focus-visible {
  outline: var(--focus-ring-width) solid var(--focus-color);
  outline-offset: var(--focus-ring-offset);
  border-radius: var(--radius-inset);
  box-shadow: 0 0 0 calc(var(--focus-ring-width) + var(--focus-ring-offset)) var(--focus-halo);
}

/* 降级：关闭漂浮/视差/循环动效，保留不依赖动画的状态变化 */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
  .map-node--floating,
  .parallax,
  .hero-particles { display: none !important; }
  /* 允许：≤150ms 的透明度过渡、静态选中态、文字状态变化 */
}

/* 内容不依赖 JS：默认可见，入场动效走 @media (prefers-reduced-motion: no-preference) + .js-ready */
```

---

## 12. 验收 10 项（逐条打勾，任一项 🔴 不通过则退回）

| # | 验收项 | 判定方法 | 通过标准 |
|---|---|---|---|
| 1 🔴 | **首屏无 C 类薪资数字** | 截图 + 逐字核对 | 首屏只出现 A 类数字（2021 / 10000+ / 300+ / 85）且全部带 `机构资料` 角标；薪资不出现在首屏 |
| 2 🔴 | **红金模板已排除** | 视觉走查 + 取色 | 无大面积正红、无金色渐变文字、无毛笔/祥云堆砌；朱砂覆盖面积 ≤ 5%；中国感由结构表达 |
| 3 | **色彩与对比度达标** | axe + 取色复核 | 正文 ≥ 4.5:1、大字 ≥ 3:1、图形/边框 ≥ 3:1；色相数 ≤ 4；无纯黑纯白大面积 |
| 4 | **字体与排版** | 三档视口截图 | 标题衬线 / 正文无衬线；正文行高 ≥ 1.7、行宽 28–38 字；无 < 12px 的合规标注；不依赖远程字体 |
| 5 | **布局节奏有差异** | 整页长截图 | 相邻 section 底色或版心不同，无连续 3 节同构；无"同尺寸同圆角同阴影"卡片组 ≥ 2 处；半径遵循内外递减 |
| 6 | **三模块互动可完成** | 手工操作 | 地图节点可点且常驻 `参考范围·以实际岗位为准`；步骤条表达顺序而非通过率、口径与时效提示同屏；陪跑时间线四段可展开、测验结果带免责 |
| 7 | **移动端可用** | 375 / 390 / 768 实测 | 无横向滚动；触控目标 ≥ 44×44；首屏 H1+CTA 可见；地图退化为列表；无 `user-scalable=no` |
| 8 🔴 | **无障碍可操作** | 纯键盘走查 + 屏幕阅读器抽查 | 全流程键盘可达；焦点环可见且不被 `outline:none` 移除；`lang="zh-CN"`；标注组建 SR 可读；面板关闭后焦点归位 |
| 9 🔴 | **动效尊重用户偏好** | 开 `prefers-reduced-motion: reduce` + 禁 JS | 降级后漂浮/视差/循环动效全停、内容完整可读；禁 JS 时 H1/CTA/数字/标注仍可见（无 `opacity:0` 白屏） |
| 10 🔴 | **合规标注在视觉上可信** | 逐条比对 `content-plan.md` §6 | 每处 C 类数字 100% 与 `参考范围·以实际岗位为准` 同视区且不折叠；B 类数字有署名来源或已删除；D 类话术零出现；未授权平台商标零出现 |

**退回机制**：第 1、2、8、9、10 项为红线，任一不通过则整版退回整改，不进入细节优化。

---

## 13. 审查声明（实际依据 / 未核验事项）

**实际查看**
- `work/xintu.txt`（心途领航机构 PDF 文本：公司简介、主营业务、师资、就业平台、市场前景与薪资、真实就业、资质、企业文化）——全文
- `work/ppt.txt`（汉教云程就业服务 PPT：四大服务支柱与承诺话术）——全文
- `work/content-plan.md`（内容分级 A/B/C/D、三模块标题 / CTA / FAQ、ICA 差异核查、数字分级总表）——全文
- `work/lead_prompt.txt`（前端主负责人任务书，含视觉方向与验收要求）
- `work/visual_prompt.txt`、`work/content_prompt.txt`、`work/launch_visual.sh`（角色与分工确认）
- 目录状态：`outputs/` 为空；`work/site/` 不存在

**未核验 / 需在验收阶段补做**
1. **实际渲染结果** —— `work/site/` 尚未创建，本文件全部内容为**落稿前设计约束**，§12 的 10 项验收均**未执行**，交付后需逐条实测并回填结论
2. **对比度实测值** —— §2.3 的比值为 sRGB 相对亮度估算，含透明度叠加后的实际值须用工具复核
3. **C 类数字的最终呈现位置** —— 需前端落稿后核对是否集中，是否与标注同视区
4. **第三方平台商标授权** —— 未确认，本文一律按"未授权则不上屏"处理
5. **案例素材** —— `xintu.txt` §4.3 / §4.4 原文为图片或空白位，无可用个案，模块三案例区按空态处理
6. **品牌主体与署名** —— "心途领航 / 汉教云程 / LUOJIA CULTURE / 师说新语 / 协会"多主体关系未确认，页脚与备案信息需业务方统一后再落
7. **字体渲染差异** —— `SimSun` 在 Windows 与 `Songti SC` 在 macOS 的字重表现不同，需在两端实机各看一次标题
